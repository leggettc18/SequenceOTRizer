#include <iostream>
#include "Sequence.h"

int main(int argc, char *argv[]) {
    if (argc == 1) {
        std::cout << "CLI tool for creating Custom Sequence Patch OTRs" << std::endl;
    }
    return 0;
}